(ns rojo.client-test
  (:require [clojure.test :refer :all]
            [rojo.client :refer :as client]
            [rojo.methods :refer :as meth]))

;; (deftest                                
;;   (testing "API call should be correctly computed"
;;     (let [token (clien)])))
