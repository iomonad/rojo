(ns rojo.client-test
  (:require [clojure.test :refer :all]
            [rojo.client :as c]
            [rojo.methods :as m]))

;; (deftest
;;   (testing "API call should be correctly computed"
;;     (let [token (clien)])))
